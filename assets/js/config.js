/* Uncommun Logic — single source of truth for anything that changes.
   Edit this file only. Nothing else hard-codes these values. */

window.UL = {
  /* Public contact. Clarence is the single point of contact. */
  email: 'Clarence.allouard@uncommunlogic.com',
  phone: '+61456810652',
  phoneDisplay: '+61 456 810 652',

  /* Google Calendar appointment schedule.
     Google Calendar > Create > Appointment schedule > Open booking page >
     Share > copy the link, then paste it here. The embeddable form of the
     link ends in "?gv=true". Leave empty and the page shows the fallback
     panel instead of an empty frame. */
  bookingUrl: '',

  /* Set to the same schedule's public page if you want the button to open a
     new tab rather than the embedded frame. Falls back to bookingUrl. */
  bookingLinkUrl: '',

  site: 'https://www.uncommunlogic.com'
};
